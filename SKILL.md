---
name: job-compass
description: 帮助应届毕业生通过问答测评找到适合的岗位方向，评估岗位的AI冲击风险，并对具体岗位做适配分析。当用户说“我不知道选什么岗位”“帮我做选岗测评”“这个岗位适合我吗”时使用。
---

# Job Compass 选岗罗盘

帮助应届毕业生完成从「不知道选什么岗位」到「判断具体岗位该不该投」的全流程选岗决策。

## 技能概述

本技能分为两个部分，两部分必须内在联动：
- **第一部分：建立用户画像** —— 通过问答采集用户信息，输出推荐岗位、AI 冲击风险评估，并把结构化用户画像报告后台保存。
- **第二部分：岗位适配分析** —— 用户输入双选会上看到的具体岗位（岗位名称或 JD），基于已保存的用户画像做逐项特质比对，输出推荐率与投递建议。

**强制联动规则**：第二部分禁止孤立分析。必须先读取第一部分保存的用户画像报告，再基于画像判断岗位适配度。未找到画像时，必须先引导用户完成第一部分。

---

## 第一部分：建立用户画像

### 模式选择规则（强制）
进入第一部分时，AI 不得直接发题，必须先做开场白并向用户提供两个选项：
- **A. 快速版（18题）**：耗时约3分钟，适合急需初步方向的用户。按 references/questionnaire.md 走题。
- **B. 专业版（80题）**：耗时约15分钟，结合霍兰德（RIASEC）与MBTI理论，生成深度画像。按 references/questionnaire_pro.md 走题。

用户选择后，必须严格走对应的题库，不得混淆。

### 第一部分流程
1. 开场白 + 让用户选快速版或专业版。
2. 根据用户选择分发对应题库：
   - 如果选快速版（18题）：依次发题，答完后输出简版画像。
   - 如果选专业版（80题）：依次发题（分三批发，每批完成后等用户说“继续”），答完后输出深度画像。
3. 生成并保存画像报告。
4. 输出三样东西（按顺序一次性给出）：
   - 输出1：5-8个具体岗位推荐（含匹配度、匹配理由、建议补充的能力、目标城市薪资范围）。
   - 输出2：每个推荐岗位的 AI 冲击风险评估（动态生成）。
   - 输出3：结构化用户画像报告。

### 差异化画像生成规则（强制）
- **若用户选快速版**：按原有逻辑输出特质排名、能力分数、匹配/排除方向。
- **若用户选专业版**：必须在画像报告中额外输出：
  1. 霍兰德代码（如 SEC 社会型+企业型+常规型），并解释三个字母的组合含义。
  2. MBTI 倾向（如 ENFJ 主人公型），并解释该类型在职场中的优劣势。
  3. 结合 RIASEC 和 MBTI 的深度分析，给出更精准的岗位推荐和短板建议。

### 输出要求
- 推荐岗位必须从 references/role_database.md 的岗位映射表中选取。
- 薪资范围根据用户填写的目标城市动态生成，不固定任何单一城市。
- AI冲击风险必须动态分析，禁止照抄静态等级。

---

## 第二部分：岗位适配分析

### 输入解析（二选一）
- 用户只输入岗位名称：先查 references/role_database.md 中该岗位的标准特质映射。
- 用户粘贴岗位 JD：优先基于 JD 文本分析，提取职责关键词。

### 执行四件事
1. **拆解岗位需要的核心特质**：提取 3-5 个核心特质，使用 references/trait_library.md 的标准化特质词。
2. **与用户画像逐项比对（双维度判定）**：
   - 强匹配（排第1 + 能力≥4）
   - 喜欢但不擅长（排第1 + 能力≤2）
   - 可匹配（排第2-3 + 能力≥4）
   - 弱匹配（排第4以后 + 能力≤2）
   - 一票否决规则：命中画像 exclude_directions 或 hard_constraints，一律按弱匹配处理。
3. **输出推荐率与理由**：推荐率公式为：实际得分 / 满分 × 100%（强匹配3分，可匹配2分，中间档1分，弱匹配0分）。
4. **让用户自行决定**：给出「投递」与「不投递」两种情形的行动建议。

### 固定解释规则（强制）
当推荐率 < 40% 且未命中一票否决时，必须在推荐率后附加说明：
> “该分数偏低不代表方向错误，而是反映当前能力尚未兑现。方向对口 + 成长空间大，仍值得投递，但需用作品/案例弥补能力证据。”

---

## 注意事项
- 薪资数据为参考值，需结合目标城市最新招聘行情核对。
- 画像报告是分析基础而非命运判决：弱匹配岗位也可投递，只是需要更充分的准备。
- 用户重新作答第一部分后，以最新画像为准。
